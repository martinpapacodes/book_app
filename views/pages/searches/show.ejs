<!DOCTYPE html>
<html lang="en">
<%- include('../../layout/head') %>

<body>
  <%- include('../../layout/header') %>
  <div id="book_container">
    <% bookResultArr.forEach(book => { %>
    <div id="book_results">
      <img src="<%= book.image_url %>" alt="" />
      <h3>Title: <%= book.title %></h3>
      <h3>Authors: <%= book.author %></h3>
      <p><%= book.description %></p>
      <h3>ISBN: <%= book.isbn %></h3>
      <h3>Bookshelf: <%= book.bookshelf %></h3>

      <button class="selectBook" id="deleteButton">Select This Book</button>
      <form action="/books" method="POST" class="addBookForm">
        <input input type="text" name="title" value="<%= book.title %>">
        <input input type="text" name="author" value="<%= book.author %>">
        <input input type="text" name="isbn" value="<%= book.isbn %>">
        <input input type="text" name="image_url" value="<%= book.image_url %>">
        <textarea name="description"><%= book.description %></textarea>
        <input input type="text" name="bookshelf" placeholder="Enter bookshelf" required>
        <input type="submit" value="Add to Database">
      </form>
    </div>
    <% }) %>
  </div>
  <script src="https://code.jquery.com/jquery-3.4.1.min.js"
    integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
  <script src="/js/app.js"></script>
</body>

</html>